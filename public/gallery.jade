.wrap
	span #[h1 Paper sliceforms] #[h4(st) designed and assembled by Yongquan Lu]
	ul.g
		for image in items
			li
				figure.centered
					.thumb
						a(href="/images/gallery/#{image.filename}", data-lightbox="moo", data-title="#{image.title} (#{image.year})")
							img(src="/images/gallery/thumbs/#{image.filename}")
							.overlay
					figcaption
						label #{image.title} (#{image.year})
						.subcaption #{image.description}
						.subcaption(style="height: 19px;")
							if image.wlpr
								a(href="/app.html?template=#{image.wlpr}") Try this out
script(src='bower/lightbox2/dist/js/lightbox.js')
